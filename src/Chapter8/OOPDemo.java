package Chapter8;

public class OOPDemo {

	public static void main(String[] args) {
		/**
		 * 1.构造方法：1）没有返回值 2）方法名和类名相同
		 * 2.不写构造方法，是有默认构造的 
		 * 3.new：分配内存的同时，调用了默认构造方法。如果定义了代参的构造方法
		 * 默认构造就被覆盖了。要想在使用默认构造，再写一遍。
		 * 4.重载：方法名一样，参数列表不一样。
		 * 5.有参数化构造，就把隐式构造覆盖了。
		 */

	}

}
